global_costmap:
  global_frame: odom # change it as map
  robot_base_frame: base_link
  transform_tolerance: 0.5
  update_frequency: 10.0
  publish_frequency: 10.0
  static_map: true
  resolution: 0.02
  cost_scaling_factor: 10.0
  inflation_radius: 0.02
